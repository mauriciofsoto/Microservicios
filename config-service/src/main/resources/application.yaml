server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/Stackft/Microservicios
          search-paths: config-data
    discovery:
      client:
        service-url:
          defaultZone: http://localhost:8761/eureka/
  application:
    name: config-service